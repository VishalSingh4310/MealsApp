class Category {
  constructor(id, title, color, textColor, icon) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.textColor = textColor;
    this.icon = icon;
  }
}

export default Category;
